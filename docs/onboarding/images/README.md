# Onboarding Images

Prefer local screenshots captured from the running Ops Board stack and onboarding playground.

Current committed captures:

- `dummy-api-health.png`: browser view of `http://localhost:18080/health`.
- `dummy-api-work.png`: browser view of `http://localhost:18080/work/demo`.

Optional future captures:

- `signoz-dummy-api-traces.png`: SigNoz traces view filtered to `dummy-api` after SigNoz login.
- `signoz-dummy-job-traces.png`: SigNoz traces view filtered to `dummy-job` after SigNoz login.

If an external official visual is used, add the source URL and capture date beside the image reference in the manual that uses it.
